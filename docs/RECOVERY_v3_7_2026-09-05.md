# v3.5–v3.7 recovery record (2026-09-05)

## Scope and provenance

This branch archives the v3.5–v3.7 files that were present in the local working tree at:

`/workspace/scratch/52dece505607/critical-slowing-patch-warning`

The recovery branch was created from:

- source branch: `feature/v2.8-patch-formation-dynamics`
- source HEAD: `945804cf54c34d54bb017ec0e132ee2acbd7f462`
- recovery branch: `archive/v3.7-recovered-2026-09-05`

Before recovery, `README.md` and `requirements.txt` contained local modifications. The v3.5–v3.7 source modules, tests, scripts, documents, and selected result folders were present locally but untracked by Git. This commit preserves that accessible working-tree state; it does not claim that those files were part of the source HEAD.

The separately exported recovery ZIP has SHA256:

`5b6c3506ea499c8b4918ab66fb5eeffbc604d18746aa31c461b1c7259baba155`

## Included material

- real-data modules under `src/real_data/`;
- v3.5–v3.7 analysis and data-download scripts;
- related unit tests;
- dependency list and repository usage documentation;
- v3.5–v3.7 research notes and data-source description;
- compact reports, decisions, summary tables, and summary figures.

Raw data, caches, virtual environments, credentials, model weights, detailed event risk sets, detailed cross-site predictions, and repeated parameter-audit output directories are excluded.

## Verification performed

The existing related tests were executed without rerunning any large experiment:

```text
python -m unittest discover -s tests -p 'test_*.py' -v
Ran 16 tests in 1.446s
OK
```

An initial attempt to use `python -m pytest` did not execute tests because `pytest` is not installed in the current runtime. The test files use `unittest`, so the standard-library discovery command above was used successfully without changing project dependencies.

## Limits

- Existing historical result values were copied as found and were not regenerated or altered.
- The successful unit tests validate the implemented small-scale logic, not full reproducibility of all historical large-data experiments.
- Raw-data-dependent analyses were not rerun.
- Dependency versions are not pinned in the recovered `requirements.txt`; no lockfile or separate environment specification was present.
- Provenance before the source HEAD cannot be reconstructed from the local Git history because the recovered v3.5–v3.7 files were originally untracked.
